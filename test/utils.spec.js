/* eslint no-unused-expressions: 0 */

var expect = require('expect')
var utils = require('../src/utils')

describe('utils', () => {

    it('should be defined', () => {
        expect(utils).toNotBe.undefined
    })

})
